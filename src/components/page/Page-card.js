import React from 'react';
import {UserCardComponent} from '../user-card.component';

export const PageCard = () => {
    return (
        <div>
            <UserCardComponent />
        </div>
    );
};
